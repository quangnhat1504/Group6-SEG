\begin{table}[htbp]
\centering
\caption{Query routing results on SciFact test (full 300 queries). All routers evaluated on the identical test split. Classical TF-IDF LogReg trained on train split (809 queries), evaluated on test split. Small LLM uses Qwen2.5-0.5B QLoRA fine-tuned on trainfit split (648 queries, dev=161 excluded) with raw log-probability routing. Oracle Router selects the best-performing retriever per query as the theoretical upper bound.}
\label{tab:routing}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lrrrrrr}
\toprule
Router & Accuracy & Macro-F1 & nDCG@10 & Recall@10 & Recall@100 & MRR@10 \\
\midrule
Random Router                       & 0.3267 & 0.2617 & 0.6290 & 0.7778 & 0.9132 & 0.5875 \\
Majority Router (always BM25)       & 0.7533 & 0.2864 & 0.6523 & 0.7757 & 0.8731 & 0.6184 \\
Classical TF-IDF LogReg (train$\to$test) & 0.2867 & 0.2504 & 0.6368 & 0.8144 & 0.9216 & 0.5880 \\
Small LLM QLoRA (LogProb, trainfit$\to$test) & 0.4167 & 0.2984 & 0.6359 & 0.7838 & 0.9112 & 0.5950 \\
\textbf{Oracle Router (upper bound)} & \textbf{1.0000} & \textbf{1.0000} & \textbf{0.7617} & \textbf{0.8711} & \textbf{0.9127} & \textbf{0.7337} \\
\bottomrule
\end{tabular}
}
\\
\vspace{4pt}
{\footnotesize Note: Training on trainfit (648q, dev excluded) yields nDCG@10 = 0.6359 --- slightly better than the old leaky split (809q incl.\ dev, nDCG@10 = 0.6270). Dev-inclusive training hurt test performance, consistent with SciFact's train/test distribution shift. Temperature+bias calibration on the held-out dev set (161q) was attempted but further degraded results (nDCG@10 = 0.5918) --- the dev set distribution does not transfer to test. In-split calibration (calibrate directly on test, diagnostic only) reached nDCG@10 = 0.6848, confirming methodology works when distribution matches.}
\end{table}
