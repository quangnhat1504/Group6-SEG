[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "seg-retrieval"
version = "0.1.0"
description = "SEG301m scientific retrieval experiments"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
